# Optional: raises the GitHub API rate limit from 60/hr to 5000/hr.
# Get one at https://github.com/settings/tokens (classic token, no scopes needed).
# Copy this file to ".env" and fill in your token. Never commit the real .env file.
VITE_GITHUB_TOKEN=
